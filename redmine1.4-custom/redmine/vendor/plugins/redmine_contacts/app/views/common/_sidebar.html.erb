<%= call_hook(:view_contacts_sidebar_top) %>

<h3><%= l(:label_module_plural) %></h3>  
<%= link_to l(:label_contact_plural), { :controller => 'contacts', :action => 'index', :project_id => @project, :set_filter => 1} %>
 |
 
<% if !(@project && !authorize_for(:contacts_tasks, :index)) %>
<%= link_to l(:label_issue_plural), { :controller => 'contacts_tasks', :action => 'index', :project_id => @project} %>
 |
<% end %>
<%= link_to l(:label_note_plural), { :controller => 'contacts', :action => 'contacts_notes', :project_id => @project} %>

<%= call_hook(:view_contacts_sidebar_bottom) %> 
